import { Box, Typography } from "@mui/material";
import React from "react";

const Header = ()=>{
    
    return(
            <Box className="app-header" >
                <Typography variant="h4" color="primary">CUANDO PASA?</Typography>
            </Box>
    );
}

export default Header;